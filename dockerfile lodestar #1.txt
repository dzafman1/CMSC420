#Ubuntu base image
FROM ubuntu:18.04

#Create working directory and copy contents to working directory
WORKDIR /app
COPY . /app

#Install git if not installed
RUN apt-get-install git && \

#Clone project repository

git clone https://github.com/zhecui/incrementalAnalysis.git

#----Intalling Polymer CLI----
RUN npm install npm@latest -g
RUN git --version
RUN npm install -g bower
RUN npm instal -g polymer-cli


